(include "\set\small.firearms.accuracy")

(define "speed"
	{speed (* 0.5 %s)}  
)

(define "accuracy_boost"
	{ZeroingAccuracy 0.9 0.8}
)
(define "recoveryTime"   
	{recoveryTime (/ 60 %r)}
)

(define "burst_assault_rifle"
	{burst
		{short 4 2}			; burst shots on long distance, rand (+-)shots
		{long 10 3}			; burst shots on short distance, rand (+-)shots
	}
)

(define "burst_triple_rifle"
	{burst
		{short 3 1}			; burst shots on long distance, rand (+-)shots
		{long 6 2}			; burst shots on short distance, rand (+-)shots
	}
)

(define "burst_auto_rifle"
	{burst
		{short 1.5 0.5}		; burst shots on long distance, rand (+-)shots
		{long 4 1}			; burst shots on short distance, rand (+-)shots
	}
)